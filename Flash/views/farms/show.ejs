<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
</head>
<body>

    <%= messages %> 
    <h1><%= farm.name %> </h1>
    <ul>
        <li>City:<%= farm.city %> </li>
        <li>Email: <%= farm.email %> </li>
        
    </ul>
    <a href="/farms">Back to farms</a>
    <a href="/farms/<%= farm._id %>/edit">Edit farms</a>

    <h2>Products</h2>
    <% for( let product of farm.products ) { %>
        <li><a href="/products/<%= product._id %>"><%= product.name %></a></li>
    <% } %>

    <br><br>
    <a href="/farms/<%= farm.id %>/products/new">New Product</a>

    <form action="/farms/<%= farm._id %>?_method=DELETE" method="POST">
        <button type="submit">delete farm</button>
    </form>
</body>
</html>